#ifndef CERTS_H
#define CERTS_H

#include "Arduino.h"

// Declarations only — definitions live in certs.cpp (single copy in binary)
extern const char newRoot_CA[];
extern const char ISRGroot_CA[];

#endif
